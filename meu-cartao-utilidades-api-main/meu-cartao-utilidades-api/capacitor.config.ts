import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Meu Cartão + Utilidades (API)',
  webDir: 'www'
};

export default config;
